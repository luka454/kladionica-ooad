<ContentControl x:Class="Kladionica.PregledPonuda"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Orientation="Vertical">
           
            <Border x:Name="borderFU" Padding="10" Background="#FFF5E5E5" IsHitTestVisible="False">
                <StackPanel>
                    <TextBlock FontSize="22" FontWeight="Bold">Fudbalske utakmice</TextBlock>
                    <ListBox x:Name="fudbalkseUtakmice" Background="{Binding Background}" FontSize="18">
                        
                    </ListBox>
                </StackPanel>
            </Border>

            <Border x:Name="borderT" Padding="10" Background="#FFF3ECCB" IsHitTestVisible="False">
                <StackPanel>
                    <TextBlock FontSize="22" FontWeight="Bold">Tenis</TextBlock>
                    <ListBox x:Name="Tenis" Background="{Binding Background}" FontSize="18">
                        
                    </ListBox>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</ContentControl>
